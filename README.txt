Steps to setup locally:
----------------------------

- cd into each folder (client/server), and run "npm install"
- create a ".env" file in the server folder, copy the contents from the ".env.local" file into your new ".env" file
    - take the values sent to you via email and use them to populate your new ".env" file

To run:
----------------------------
- cd into server folder and run "npm run full"